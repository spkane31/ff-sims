// component/theme.js

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#228B22", // Olive Green
    },
    secondary: {
      main: "#ff5722", // Example secondary color
    },
  },
});

export default theme;
